/**
 * author: Aryan Suri
 * licence: MIT
 **/
#include <iostream>
using namespace std;
const int T = 1000;

int p1() { return 0; }
int p2() { return 0; }

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t = T;
  while (t--) {
    vector<int> a;
    while (true) {
      int x;
      cin >> x;
      a.push_back(x);
      char c;
      cin >> c;
      if (c == '\n') {
        break;
      }
    }
  }

  return 0;
}
